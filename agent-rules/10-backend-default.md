# 10 - Backend Default

Default role: backend-first implementation agent for Cargo Agent.

- Use `docs/agent/backend-implementation-agent.md` as operating profile.
- Prefer correctness and explicit invariants over speed.
- Keep changes MVP-pragmatic and production-aware.
- If uncertain, mark assumptions and point to canonical docs.
- Do not invent behavior that conflicts with contracts or context docs.

